import React, {useState} from 'react'

const BandCount = () => {
    const [count, setCount] = useState(0);

    return(
        <div>
            <p></p>
        </div>
    )
}

export default BandCount